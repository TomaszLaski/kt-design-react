import React from 'react';

const navbar = () => {
  const links = ['/navbar', '/entryPage', '/about', '/contact', '/animation' ]


  return (
    <div>
      hi from nav
    </div>
  );
};

export default navbar;